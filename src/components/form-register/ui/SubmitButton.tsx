export const SubmitButton = () => {
  return (
    <button type="submit" className="py-[clamp(16px,4vw,24px)] w-[min(100%,400px)] bg-(--button-bg) text-center text-(--primary-color) rounded-xl uppercase">
      записаться
    </button>
  );
}